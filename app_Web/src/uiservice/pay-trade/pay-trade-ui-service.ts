import PayTradeUIServiceBase from './pay-trade-ui-service-base';

/**
 * 支付交易UI服务对象
 *
 * @export
 * @class PayTradeUIService
 */
export default class PayTradeUIService extends PayTradeUIServiceBase {

    /**
     * Creates an instance of  PayTradeUIService.
     * 
     * @param {*} [opts={}]
     * @memberof  PayTradeUIService
     */
    constructor(opts: any = {}) {
        super(opts);
    }

}
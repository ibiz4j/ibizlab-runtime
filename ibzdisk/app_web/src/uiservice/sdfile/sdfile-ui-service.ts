import SDFileUIServiceBase from './sdfile-ui-service-base';

/**
 * 文件UI服务对象
 *
 * @export
 * @class SDFileUIService
 */
export default class SDFileUIService extends SDFileUIServiceBase {

    /**
     * Creates an instance of  SDFileUIService.
     * 
     * @param {*} [opts={}]
     * @memberof  SDFileUIService
     */
    constructor(opts: any = {}) {
        super(opts);
    }

}